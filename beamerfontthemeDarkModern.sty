%% DarkModern color-theme for LaTeX Beamer
%% By Jan Niklas Schmäke created on 21.12.2024
%% https://github.com/schmaeke/BeamerDarkModern

% Load packages
\RequirePackage{antpolt}
\RequirePackage[familydefault]{Chivo}
\RequirePackage[mono]{inconsolata}
\RequirePackage[T1]{fontenc}

% Title frame
\setbeamerfont{title}{family = \rmfamily, size = {\fontsize{24pt}{24pt}\selectfont}, series = \bfseries}
\setbeamerfont{subtitle}{family = \sffamily, size = \large, series = \normalfont}

\setbeamerfont{author}{family = \sffamily, size = \Huge, series = \normalfont}
\setbeamerfont{institute}{family = \sffamily, size = \large, series = \normalfont}

% Normal text
\setbeamerfont{normal text}{family = \sffamily}

% Outer theme
\setbeamerfont{frametitle}{family = \rmfamily, size = \huge, series = \bfseries}
\setbeamerfont{footline}{family = \sffamily, size = \footnotesize, series = \normalfont}

% Itemize & Enumerate
\setbeamerfont{itemize/enumerate body}{size = \normalsize}
\setbeamerfont{itemize/enumerate subbody}{size = \normalsize}
\setbeamerfont{itemize/enumerate subsubbody}{size = \normalsize}

% TOC
\setbeamerfont{section in toc}{family = \sffamily, size  = \large, series = \bfseries}
\setbeamerfont{subsection in toc}{family = \sffamily, series = \normalfont}
\setbeamerfont{subsubsection in toc}{family = \sffamily, series = \itshape}

% Blocks
\setbeamerfont*{block title}{family = \sffamily, series = \bfseries}
